/**
 * Contém as mensagens de exceção para o módulo de refresh token.
 */
export const REFRESH_EXCEPTIONS = {
  REFRESH_TOKEN_INVALID: 'Invalid refresh token.',
  REFRESH_TOKEN_EXPIRED: 'Refresh token expired.',
};
