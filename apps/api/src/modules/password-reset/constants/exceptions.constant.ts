/**
 * Contém as mensagens de exceção para o módulo de redefinição de senha.
 */
export const PASSWORD_RESET_EXCEPTIONS = {
  RECOVERY_TOKEN_INVALID: 'Invalid recovery token.',
  RECOVERY_TOKEN_EXPIRED: 'Recovery token expired.',
};
